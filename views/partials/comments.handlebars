<div class="commnets">
  {{#each this}}
  <div class="'card">
    <h3 class="'class-header"></h3>
    <section class="card-body comment">
      <div class="text">
        {{comment_text}}
      </div>
      <div class="meta">
        {{user.username}}, {{format_date date_created}}
      </div>
    </section>
  </div>
  {{/each}}
</div>